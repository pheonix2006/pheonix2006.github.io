interface:
  display_name: "Humanizer zh"
  short_description: "Make Chinese portfolio writing sound natural."
  default_prompt: "Use $humanizer-zh to rewrite this Chinese portfolio text so it sounds natural while preserving all facts."
policy:
  allow_implicit_invocation: true
